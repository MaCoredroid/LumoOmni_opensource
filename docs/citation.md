# Citation

If you publish work based on this project, add a citation entry like the following.

> Replace fields (authors, year, URL, version) with your actual metadata.

```bibtex
@misc{lumoOmni2026,
  title        = {LumoOmni: Staged construction of an omni-modal model from a text-only LLM backbone},
  author       = {Your Name and Collaborators},
  year         = {2026},
  howpublished = {Project documentation and code},
  note         = {Version: v0.1 (living document)}
}
```

## What to cite precisely

- The project version / git commit hash
- The `token_space.json` hash (for token‑LM results)
- The exact base model identifier for any adapter checkpoints
